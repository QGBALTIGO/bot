# Validação

Antes de mergear esta leva, o PR deve passar pelo `System audit` e pelo `AniNexus frontend`. Após o merge em `main`, Railway e `Production smoke` precisam ficar verdes antes de considerar a correção publicada.
